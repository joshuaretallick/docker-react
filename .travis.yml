language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t joshuaretallick/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true joshuaretallick/docker-react npm run test
